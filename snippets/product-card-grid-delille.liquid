{% style %}
    /* Order top proposiotions section */

    .order-container{
    padding: 20px;
    }
    .order > article{
    width: 430px;
    color: white;
    }
    .order > article:nth-child(odd){
    float: left;
    }
    .order > article:nth-child(even){
    float: right;
    }
    .order-item{
    background-color: rgb(45, 45, 45);
    padding-bottom: 20px;
    display: table;
    }
    .order-item h3{
    text-align: center;
    font-size: 24px;
    font-weight: normal;
    line-height: 20px;
    }
    .order-item p{
    font: 14px/24px "Neutra Text";
    padding: 0 25px;
    margin: 10px auto 20px;
    }
    .order-item table,
    .order-item form,
    .order-item img:nth-last-child(1){
    float: left;
    }
    .order-item table,
    .order-item form{
    font-size: 18px;
    }
    .order-item table{
    text-transform: capitalize;
    margin-left: 25px;
    }
    .order-item form{
    margin-left: 50px;
    font-family: "Neutra Text";
    text-transform: uppercase;
    }
    .order-item input{
    width: 40px;
    height: 30px;
    padding-left: 15px;
    margin: 0 0 20px 10px;
    border: none;
    box-shadow: inset 1px 2px 5px 0px rgba(0, 1, 1, 0.2);
    }
    input[type=number]::-webkit-inner-spin-button,
    input[type=number]::-webkit-outer-spin-button {
    opacity: 1;
    }
    .order-item form button{
    padding: 0;
    text-transform: uppercase;
    border: none;
    color: white;
    background-color: rgb(45, 45, 45);
    }
    .order-item form button:hover{
    cursor: pointer;
    }
    .order-item img:nth-last-child(1){
    margin: -5px 0px 0px 27px;
    max-height: 90px;
    }
    .order-item img:nth-last-child(1):hover{
    transform: scale(1.02);
    transition: 500ms ease-in-out;
    }
    .quantity{
    display: none;
    }
{%endstyle%}

<div class="grid-view-item{% unless product.available %} grid-view-item--sold-out{% endunless %} product-card order-container white-transparent top-margined">
    {% comment %}<a class="grid-view-item__link grid-view-item__image-container product-card__link"{% endcomment %}
       {% comment %}href="{{ product.url | within: collection }}">{% endcomment %}
        {% comment %}<span class="visually-hidden">{{ product.title }}</span>{% endcomment %}
    {% comment %}</a>{% endcomment %}

    {% capture img_id %}ProductCardImage-{{ section.id }}-{{ product.id }}{% endcapture %}
    {% capture wrapper_id %}ProductCardImageWrapper-{{ section.id }}-{{ product.id }}{% endcapture %}
    {%- assign img_url = product.featured_image | img_url: '1x1' | replace: '_1x1.', '_{width}x.' -%}

    {% unless product.featured_image == blank %}
        {% include 'image-style' with image: product.featured_image, width: max_height, height: max_height, small_style: true, wrapper_id: wrapper_id, img_id: img_id %}
    {% endunless %}
    <div class="h4 grid-view-item__title" aria-hidden="true">{% cycle "Meet the", "Order Yours" %} {% cycle "2010", "2012" %} {{ product.title }} {% cycle "Today", "" %}!</div>
    <div id="{{ wrapper_id }}" class="grid-view-item__image-wrapper js">
        <div style="padding-top:{% unless product.featured_image == blank %}{{ 1 | divided_by: product.featured_image.aspect_ratio | times: 100}}%{% else %}100%{% endunless %};">
            <img id="{{ img_id }}"
                 class="grid-view-item__image lazyload"
                 src="{{ product.featured_image | img_url: '300x300' }}"
                 data-src="{{ img_url }}"
                 data-widths="[180, 360, 540, 720, 900, 1080, 1296, 1512, 1728, 2048]"
                 data-aspectratio="{{ product.featured_image.aspect_ratio }}"
                 data-sizes="auto"
                 alt="">
        </div>
    </div>
    <p class="" aria-hidden="true"> {{ product.description }} </p>
    <table>
        <tr>
            <td colspan="3">price</td>
        </tr>
        <tr>
            <td>bottle</td>
            <td>/</td>
            <td>case</td>
        </tr>
        <tr>
            <td>{% include 'product-price' %}</td>
            <td>/</td>
            <td>{{ product.price | times: 12 | money}}</td>
        </tr>
    </table>
    <form action="">
        <label for="quantity1">qty</label>
        <div class="quantity less">–</div>
        <input type="number" id="quantity1" name="quantity" value="1" min="1" max="100"><br>
        <div class="quantity more">+</div>
        <button type="submit" value="Submit">buy now</button>
    </form>
    <a href=""><img class="btn" src="./img/button_buy.png" alt="buy de lille wine"></a>

    <noscript>
        {% capture image_size %}{{ max_height }}x{{ max_height }}{% endcapture %}
        <img class="grid-view-item__image" src="{{ product.featured_image.src | img_url: image_size, scale: 2 }}"
             alt="{{ product.featured_image.alt }}"
             style="max-width: {{ max_height | times: product.featured_image.aspect_ratio }}px;">
    </noscript>


    <dl>
        {% if section.settings.show_vendor %}
            <div class="grid-view-item__vendor">
                <dt>
                    <span class="visually-hidden">{{ 'products.product.vendor' | t }}</span>
                </dt>
                <dd>
                    {{ product.vendor }}
                </dd>
            </div>
        {% endif %}


    </dl>
</div>



