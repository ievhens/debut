<div class="page-width">
    <div class="grid">
        <div class="grid__item medium-up--five-sixths medium-up--push-one-twelfth">
            <div class="section-header text-center">
                <h1>{{ page.title }}</h1>
            </div>
            {{ page.content }}
            {% section "delille-info" %}
            {% section "delille-collection" %}
            {% section "delille-collection-list" %}
        </div>
    </div>
</div>
